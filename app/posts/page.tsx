import Link from "next/link";
import { Plus, RotateCcw } from "lucide-react";
import { AppShell } from "@/components/AppShell";
import { getSql, hasDatabaseConfig, withDatabaseRetry } from "@/lib/db";
export const dynamic="force-dynamic";
function formatTime(v:any){return v?new Intl.DateTimeFormat("cs-CZ",{dateStyle:"medium",timeStyle:"short",timeZone:"Europe/Prague"}).format(new Date(v)):"Bez termínu"}
export default async function Posts(){let rows:any[]=[];let databaseError:string|null=null;if(hasDatabaseConfig()){try{const sql=getSql();rows=await withDatabaseRetry(()=>sql`SELECT p.id,p.title,p.status,p.media_type,p.media_urls,p.scheduled_at,c.name client_name,COALESCE(json_agg(json_build_object('platform',pt.platform,'status',pt.status,'error',pt.error_message)) FILTER (WHERE pt.id IS NOT NULL),'[]') targets FROM posts p JOIN clients c ON c.id=p.client_id LEFT JOIN post_targets pt ON pt.post_id=p.id GROUP BY p.id,c.name ORDER BY p.created_at DESC`)}catch(error){console.error("Posts database error:",error);databaseError="Databáze je dočasně nedostupná."}}
return <AppShell title="Obsah" subtitle="Přehled konceptů, naplánovaných i publikovaných příspěvků." action={<Link href="/posts/new" className="button primary"><Plus size={17}/> Nový příspěvek</Link>}><div className="panel">{rows.length?<div className="table-wrap"><table className="data-table"><thead><tr><th>Obsah</th><th>Termín</th><th>Platformy</th><th>Stav</th></tr></thead><tbody>{rows.map(row=>{const first=Array.isArray(row.media_urls)?row.media_urls[0]:null;return <tr key={row.id}><td><div className="post-cell">{first?.url?<img src={first.url} className="media-mini" alt=""/>:<span className="media-mini"/>}<div><strong>{row.title}</strong><small>{row.client_name} · {row.media_type}</small></div></div></td><td>{formatTime(row.scheduled_at)}</td><td>{(row.targets||[]).map((t:any)=><span key={t.platform} className={`badge ${t.status}`} style={{marginRight:6}}>{t.platform}</span>)}</td><td><span className={`badge ${row.status}`}>{row.status}</span>{row.status==="failed"&&<form action={`/api/posts/${row.id}/retry`} method="post" style={{display:"inline",marginLeft:8}}><button className="button" title="Zkusit znovu"><RotateCcw size={14}/></button></form>}</td></tr>})}</tbody></table></div>:<div className="empty">Žádný obsah. Vytvořte první příspěvek.</div>}</div></AppShell>}
