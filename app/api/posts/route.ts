import { NextResponse } from "next/server";
import { z } from "zod";
import { getSql,hasDatabaseConfig } from "@/lib/db";
const asset=z.object({url:z.string().url(),publicId:z.string().min(1),resourceType:z.enum(["image","video","raw"]),originalFilename:z.string().optional(),bytes:z.number().optional()});
const schema=z.object({client_id:z.string().uuid(),title:z.string().trim().min(2).max(160),brief:z.string().max(4000).default(""),caption:z.string().trim().min(2).max(10000),media_type:z.enum(["photo","reel","carousel","story"]),media_urls:z.array(asset).min(1).max(10),scheduled_at:z.string().datetime(),status:z.enum(["draft","scheduled","pending_approval"]).default("scheduled"),platforms:z.array(z.enum(["instagram","tiktok"])).min(1)}).superRefine((v,c)=>{if(v.media_type==="reel"&&v.media_urls[0]?.resourceType!=="video")c.addIssue({code:"custom",path:["media_type"],message:"Reel vyžaduje video."});if(v.media_type==="photo"&&v.media_urls[0]?.resourceType!=="image")c.addIssue({code:"custom",path:["media_type"],message:"Fotografie vyžaduje obrázek."});if(v.media_type==="carousel"&&v.media_urls.length<2)c.addIssue({code:"custom",path:["media_urls"],message:"Carousel vyžaduje alespoň dvě média."})});
export async function GET(){if(!hasDatabaseConfig())return NextResponse.json({data:[],demo:true});try{const sql=getSql();const rows=await sql`SELECT p.*,json_build_object('name',c.name) clients FROM posts p JOIN clients c ON c.id=p.client_id ORDER BY p.created_at DESC`;return NextResponse.json({data:rows})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Database error"},{status:500})}}
export async function POST(req:Request){try{const input=schema.parse(await req.json());if(!hasDatabaseConfig())return NextResponse.json({error:"DATABASE_URL není nastavené."},{status:503});const sql=getSql();const clients=await sql`SELECT requires_approval FROM clients WHERE id=${input.client_id}::uuid`;if(!clients[0])return NextResponse.json({error:"Klient neexistuje."},{status:404});const finalStatus=input.status==="scheduled"&&clients[0].requires_approval?"pending_approval":input.status;const rows=await sql`INSERT INTO posts(client_id,title,brief,caption,media_type,media_urls,scheduled_at,status) VALUES(${input.client_id}::uuid,${input.title},${input.brief},${input.caption},${input.media_type},${JSON.stringify(input.media_urls)}::jsonb,${input.scheduled_at}::timestamptz,${finalStatus}::post_status) RETURNING *`;const post=rows[0];for(const platform of input.platforms){await sql`INSERT INTO post_targets(post_id,platform,target_format,status) VALUES(${post.id}::uuid,${platform}::platform_name,${input.media_type},${finalStatus}::post_status) ON CONFLICT(post_id,platform) DO UPDATE SET target_format=EXCLUDED.target_format,status=EXCLUDED.status,error_message=NULL`}await sql`INSERT INTO audit_log(entity_type,entity_id,action,details) VALUES('post',${post.id}::uuid,'created',${JSON.stringify({status:finalStatus,platforms:input.platforms})}::jsonb)`;return NextResponse.json({data:post},{status:201})}catch(e){if(e instanceof z.ZodError)return NextResponse.json({error:e.issues.map(i=>i.message).join(" ")},{status:400});return NextResponse.json({error:e instanceof Error?e.message:"Invalid input"},{status:500})}}
