import { Instagram, Link2, ShieldCheck } from "lucide-react";
import { AppShell } from "@/components/AppShell";
import { getSql,hasDatabaseConfig,withDatabaseRetry } from "@/lib/db";
export const dynamic="force-dynamic";
export default async function Settings(){let clients:any[]=[];let databaseError:string|null=null;if(hasDatabaseConfig()){try{const sql=getSql();clients=await withDatabaseRetry(()=>sql`SELECT c.id,c.name,sa.account_name,sa.connection_status,sa.token_expires_at FROM clients c LEFT JOIN social_accounts sa ON sa.client_id=c.id AND sa.platform='instagram' ORDER BY c.name`)}catch(error){console.error("Settings database error:",error);databaseError="Databáze je dočasně nedostupná."}}
return <AppShell title="Nastavení" subtitle="Připojení účtů a kontrola technických služeb."><div className="grid dashboard-grid"><div className="panel"><div className="panel-header"><div><h2>Instagram účty</h2><p>Jednorázové připojení pro automatické publikování</p></div></div><div className="activity-list">{clients.map(c=><div className="activity-item" key={c.id}><span className="metric-icon"><Instagram size={16}/></span><div style={{display:"flex",alignItems:"center",justifyContent:"space-between",gap:12}}><div><p>{c.name}</p><small>{c.connection_status==="connected"?`Připojeno jako ${c.account_name}`:"Účet není připojen"}</small></div>{c.connection_status==="connected"?<span className="badge published">Připojeno</span>:<a className="button primary" href={`/api/auth/instagram/start?clientId=${c.id}`}><Link2 size={15}/> Připojit</a>}</div></div>)}</div></div><div className="panel form-card"><div className="section-heading"><div><h2>Systém</h2><p>Produkční provoz bez tunnelu.</p></div></div><div className="activity-list" style={{padding:0}}><div className="activity-item"><span className="status-dot"/><div><p>Neon database</p><small>Klienti, obsah a stavy publikování</small></div></div><div className="activity-item"><span className="status-dot"/><div><p>Cloudinary</p><small>Veřejná média pro Instagram API</small></div></div><div className="activity-item"><span className="status-dot"/><div><p>Vercel Cron</p><small>Automatická kontrola naplánovaných příspěvků</small></div></div></div><div className="notice info" style={{marginTop:14}}><ShieldCheck size={15} style={{verticalAlign:"middle",marginRight:6}}/>Pro produkci nastavte stabilní APP_URL a META_REDIRECT_URI.</div></div></div></AppShell>}
